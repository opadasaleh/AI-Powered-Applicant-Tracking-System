import { title } from 'process'
import React from 'react'

export const meta = () => ([
    { title: 'Resumind | Auth' },
    {name: 'description ', content:'Log into your account '},
])

const auth = () => {
  return (
    <div>auth</div>
  )
}

export default auth